name: Publish Docker

on:
  push:
    branches: master | main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master | actions/checkout@main
      - name: Publish to registry
        uses: elgohr/Publish-Docker-Github-Action@master | elgohr/Publish-Docker-Github-Action@main
        with:
          repository: stats21220/nest
          registry: docker.pkg.github.com
          name: docker.pkg.github.com/stats21220/nest/nest
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          tags: 'develop'

